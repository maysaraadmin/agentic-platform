{
  "name": "agentic-platform",
  "version": "0.0.0",
  "scripts": {
    "start": "nx serve host",
    "build": "nx build host",
    "test": "nx test host",
    "lint": "nx lint host",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "backend:start": "cd apps/backend && uvicorn src.main:app --reload",
    "backend:test": "cd apps/backend && pytest"
  },
  "private": true
}
